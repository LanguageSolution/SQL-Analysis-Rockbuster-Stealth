--*aggregates for Release_year column*--
SELECT MIN(release_year) AS min_release_year,
	   MAX(release_year) AS max_release_year,
	   AVG(release_year) AS avg_release_year,
	   COUNT(release_year) AS Count_release_year,
	   COUNT(*) AS Row_count
  FROM film
